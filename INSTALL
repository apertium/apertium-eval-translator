Installation procedure for apertium-eval-translator:

1. Make sure that perl is properly installed

2. Execute the install.sh script as root. If no path
   is provided the script will be installed in /usr/local/bin

   # ./install.sh

Good luck!


If you want to use the alternative apertium-eval script, just copy it to a
directory in your PATH.